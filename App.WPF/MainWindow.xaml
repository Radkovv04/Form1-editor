<Window x:Class="AppWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Background="#F0F0F0">
            <Button Content="Undo" Command="{Binding UndoCommand}" Margin="5" Padding="10,2"/>
            <Button Content="Redo" Command="{Binding RedoCommand}" Margin="5" Padding="10,2"/>
            <Button Content="Save to DB" Command="{Binding SaveToDatabaseCommand}" Margin="5" Padding="10,2" Background="LightGreen"/>
        </StackPanel>

        <GroupBox Header="History" DockPanel.Dock="Right" Width="180">
            <ListBox ItemsSource="{Binding Revisions}" DisplayMemberPath="SavedAt" />
        </GroupBox>

        <TextBox Text="{Binding CurrentText, UpdateSourceTrigger=PropertyChanged}" 
             AcceptsReturn="True" 
             AcceptsTab="True" 
             FontSize="14" 
             VerticalScrollBarVisibility="Auto"
             Margin="5"/>
    </DockPanel>
</Window>
